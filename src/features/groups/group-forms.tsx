"use client";
import{useActionState,useState}from"react";
import{createGroupAction,deleteGroupAction,joinGroupAction,leaveGroupAction,regenerateInviteAction,removeMemberAction,renameGroupAction,type GroupActionState}from"@/app/group-actions";
import{Button}from"@/components/ui/button";
const initial:GroupActionState={status:"idle"};
function Status({state}:{state:GroupActionState}){return state.message?<p className="mt-2 text-sm text-coral-dark" role="alert">{state.message}</p>:null}
export function CreateGroupForm(){const[s,a,p]=useActionState(createGroupAction,initial);return<form action={a} className="grid gap-3 sm:grid-cols-[1fr_auto]"><label className="text-sm font-semibold text-ink">Group name<input className="field mt-2" maxLength={100} name="name" required/></label><Button className="self-end" disabled={p} type="submit">{p?"Creating…":"Create group"}</Button><Status state={s}/></form>}
export function JoinGroupForm({token}:{token:string}){const[s,a,p]=useActionState(joinGroupAction,initial);return<form action={a}><input name="token" type="hidden" value={token}/><Button disabled={p} type="submit">{p?"Joining…":"Join this group"}</Button><Status state={s}/></form>}
export function OwnerGroupControls({groupId,name,inviteUrl,members}:{groupId:string;name:string;inviteUrl:string;members:Array<{username:string;displayName:string;role:string}>}){const[rename,renameAction,renaming]=useActionState(renameGroupAction,initial);const[regen,regenAction,regenerating]=useActionState(regenerateInviteAction,initial);const[remove,removeAction,removing]=useActionState(removeMemberAction,initial);const[del,deleteAction,deleting]=useActionState(deleteGroupAction,initial);const[confirm,setConfirm]=useState(false);const[copyState,setCopyState]=useState<"idle"|"copied"|"error">("idle");return<div className="space-y-7"><div><h2 className="text-lg font-semibold text-ink">Invite someone</h2><p className="mt-1 text-sm text-muted">Only you can see and regenerate this link.</p><div className="mt-3 flex gap-2"><input aria-label="Invite link" className="field min-w-0" readOnly value={inviteUrl}/><button className="shrink-0 text-sm font-semibold text-coral underline underline-offset-4" onClick={async()=>{try{await navigator.clipboard.writeText(inviteUrl);setCopyState("copied")}catch{setCopyState("error")}}} type="button">{copyState==="copied"?"Copied":"Copy"}</button></div><span aria-live="polite" className={copyState==="error"?"mt-2 block text-xs text-coral-dark":"sr-only"}>{copyState==="copied"?"Invite link copied":copyState==="error"?"Copy failed. Select the link and copy it manually.":""}</span><form action={regenAction} className="mt-3"><input name="groupId" type="hidden" value={groupId}/><button className="text-xs text-muted underline underline-offset-4 disabled:cursor-wait" disabled={regenerating} type="submit">{regenerating?"Regenerating…":"Regenerate link"}</button><Status state={regen}/></form></div><form action={renameAction}><input name="groupId" type="hidden" value={groupId}/><label className="text-sm font-semibold text-ink">Rename group<input className="field mt-2" defaultValue={name} maxLength={100} name="name" required/></label><Button className="mt-3" disabled={renaming} type="submit">{renaming?"Saving…":"Save name"}</Button><Status state={rename}/></form>{members.some(m=>m.role==="member")&&<form action={removeAction}><input name="groupId" type="hidden" value={groupId}/><label className="text-sm font-semibold text-ink">Remove a member<select className="field mt-2" name="username">{members.filter(m=>m.role==="member").map(m=><option key={m.username} value={m.username}>{m.displayName} (@{m.username})</option>)}</select></label><Button className="mt-3" disabled={removing} type="submit">{removing?"Removing…":"Remove member"}</Button><Status state={remove}/></form>}<div className="border-t border-line pt-5">{confirm?<form action={deleteAction}><input name="groupId" type="hidden" value={groupId}/><p className="text-sm text-coral-dark">Delete this group and all memberships? Study sessions and goals will remain untouched.</p><div className="mt-3 flex gap-4"><Button disabled={deleting} type="submit">{deleting?"Deleting…":"Delete group"}</Button><button disabled={deleting} onClick={()=>setConfirm(false)} type="button">Cancel</button></div><Status state={del}/></form>:<button className="text-sm text-muted underline underline-offset-4" onClick={()=>setConfirm(true)} type="button">Delete group</button>}</div></div>}
export function LeaveGroupForm({groupId}:{groupId:string}){const[s,a,p]=useActionState(leaveGroupAction,initial);const[c,setC]=useState(false);return c?<form action={a}><input name="groupId" type="hidden" value={groupId}/><p className="text-sm text-muted">Leave this group? Your study history will not be deleted.</p><div className="mt-3 flex gap-4"><Button disabled={p} type="submit">{p?"Leaving…":"Leave group"}</Button><button disabled={p} onClick={()=>setC(false)} type="button">Cancel</button></div><Status state={s}/></form>:<button className="text-sm text-muted underline underline-offset-4" onClick={()=>setC(true)} type="button">Leave group</button>}
